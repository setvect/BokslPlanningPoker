# 아키텍처 문서

복슬 플래닝 포커의 프로젝트 구조, 설계 패턴, 주요 기술적 결정사항을 설명합니다.

## 📋 프로젝트 개요

- **목적**: 최소한의 기능을 가진 웹 기반 플래닝 포커 도구
- **핵심 원칙**:
  - 최대한 간단하게 구현
  - DBMS 없이 메모리 기반 데이터 저장
  - 로그인/인증 시스템 없음
  - 실시간 동기화 우선

## 🏗️ 시스템 아키텍처

### 전체 구조도

```
┌─────────────────────────────────────────────────────────────┐
│                        Client (Browser)                      │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  React 18 + TypeScript                                 │ │
│  │  - Components (UI)                                     │ │
│  │  - Hooks (useSocket, useGame, useTheme)                │ │
│  │  - Tailwind CSS (Styling + Dark Mode)                 │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ Socket.io WebSocket
                            │ (실시간 양방향 통신)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    Server (Node.js)                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Express + Socket.io                                   │ │
│  │  - Socket Handlers (게임 로직)                          │ │
│  │  - Models (Room, User, Game)                           │ │
│  │  - In-Memory Storage (Map 기반)                        │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ 공유 타입
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                   Shared Types & Constants                   │
│  - types.ts (공통 데이터 타입)                               │
│  - socket-events.ts (이벤트 정의)                            │
│  - constants.ts (게임 설정)                                  │
└─────────────────────────────────────────────────────────────┘
```

## 📁 디렉토리 구조

```
BokslPlanningPoker/
├── server/                    # 백엔드 애플리케이션
│   ├── src/
│   │   ├── index.ts          # 서버 엔트리포인트
│   │   ├── socket/           # Socket.io 이벤트 핸들러
│   │   │   └── handlers.ts   # 게임 로직 구현
│   │   ├── models/           # 데이터 모델
│   │   │   ├── Room.ts       # 방 모델 및 관리
│   │   │   ├── User.ts       # 사용자 모델
│   │   │   └── Game.ts       # 게임 상태 관리
│   │   └── utils/            # 유틸리티 함수
│   │       └── cleanup.ts    # 비활성 방 정리
│   ├── package.json
│   └── tsconfig.json
├── client/                   # 프론트엔드 애플리케이션
│   ├── src/
│   │   ├── App.tsx          # 메인 애플리케이션 컴포넌트
│   │   ├── main.tsx         # React 엔트리포인트
│   │   ├── components/      # React UI 컴포넌트
│   │   │   ├── MainPage.tsx      # 메인 화면
│   │   │   ├── GameRoom.tsx      # 게임 화면
│   │   │   ├── CardDeck.tsx      # 카드 선택 영역
│   │   │   ├── PlayerList.tsx    # 참여자 목록
│   │   │   └── GameResult.tsx    # 결과 표시
│   │   ├── hooks/           # 커스텀 React 훅
│   │   │   ├── useSocket.ts      # Socket.io 연결 관리
│   │   │   ├── useGame.ts        # 게임 상태 관리
│   │   │   └── useTheme.ts       # 테마 관리
│   │   ├── types/           # 클라이언트 전용 타입
│   │   └── styles/          # 글로벌 스타일
│   ├── package.json
│   ├── vite.config.ts
│   └── tailwind.config.js
├── shared/                  # 공유 타입 및 상수
│   ├── types.ts             # 서버-클라이언트 공통 타입
│   ├── socket-events.ts     # Socket.io 이벤트 정의
│   ├── constants.ts         # 게임 설정 상수
│   └── index.ts             # 통합 export
├── scripts/                 # 빌드 및 배포 스크립트
└── docs/                    # 프로젝트 문서
```

## 🔄 데이터 흐름

### 1. 방 생성/입장 플로우

```
Client                          Server
  │                               │
  │  createRoom(roomName, userName) │
  ├──────────────────────────────>│
  │                               │ 1. 방 ID 생성
  │                               │ 2. Room 객체 생성
  │                               │ 3. User 추가
  │                               │ 4. Socket room join
  │                               │
  │  { success, roomId, user }    │
  │<──────────────────────────────┤
  │                               │
  │  broadcast: room_update        │
  │<──────────────────────────────┤ (모든 참여자에게)
  │                               │
```

### 2. 카드 선택 플로우

```
Client                          Server
  │                               │
  │ (로컬 상태 즉시 업데이트)      │  [Optimistic Update]
  │                               │
  │  selectCard(roomId, card)     │
  ├──────────────────────────────>│
  │                               │ 1. 사용자 검증
  │                               │ 2. 카드 선택 저장
  │                               │ 3. 게임 상태 업데이트
  │                               │
  │  callback({ success })        │
  │<──────────────────────────────┤
  │                               │
  │  broadcast: user_update        │
  │<──────────────────────────────┤ (모든 참여자에게)
  │                               │
```

### 3. 카드 공개 플로우

```
Client                          Server
  │                               │
  │  revealCards(roomId)          │
  ├──────────────────────────────>│
  │                               │ 1. 게임 상태 → REVEALED
  │                               │ 2. 평균값 계산
  │                               │ 3. 결과 생성
  │                               │
  │  broadcast: game_revealed     │
  │<──────────────────────────────┤ (모든 참여자에게)
  │                               │
  │  { state, results, average }  │
  │                               │
```

## 🎯 핵심 설계 패턴

### 1. 모노레포 구조

**선택 이유:**
- 클라이언트와 서버가 타입을 공유하여 타입 안전성 보장
- 단일 저장소로 관리 용이
- 공통 상수 및 이벤트 정의 중복 제거

**구현:**
- `shared/` 디렉토리를 상대 경로로 import
- TypeScript 컴파일러가 타입 체크
- 빌드 시 각각 독립적으로 번들링

### 2. 실시간 통신 패턴

**요청-응답-브로드캐스트 패턴:**

```typescript
// 클라이언트: Optimistic Update → 서버 요청
selectCard(card)
  → 로컬 상태 즉시 업데이트 (UI 반응성)
  → socket.emit('select_card', {roomId, card}, callback)

// 서버: 처리 → 콜백 → 브로드캐스트
  → 검증 및 상태 업데이트
  → callback({success: true})
  → io.to(roomId).emit('user_update', updatedUsers)
```

**이점:**
- 즉각적인 UI 피드백 (Optimistic Update)
- 서버 검증 후 확정
- 모든 클라이언트 자동 동기화

### 3. 상태 관리

#### 서버 상태 (인메모리)

```typescript
// GameStore 클래스
class GameStore {
  private rooms: Map<string, Room> = new Map();

  // O(1) 조회
  getRoom(roomId: string): Room | undefined {
    return this.rooms.get(roomId);
  }
}

// Room 모델
class Room {
  id: string;
  name: string;
  users: Map<string, User>;  // O(1) 사용자 조회
  game: Game;
  createdAt: Date;
  lastActivity: Date;  // 비활성 방 정리용
}
```

**특징:**
- 데이터베이스 없음 - 메모리만 사용
- Map 자료구조로 O(1) 성능
- 서버 재시작 시 모든 데이터 초기화
- 5분마다 1시간 이상 비활성 방 자동 삭제

#### 클라이언트 상태 (React Hooks)

```typescript
// useGame 훅: 게임 상태 관리
const useGame = (socket: Socket | null) => {
  const [roomState, setRoomState] = useState<RoomState | null>(null);
  const [myCard, setMyCard] = useState<string | null>(null);

  // Optimistic Update
  const selectCard = async (card: string) => {
    setMyCard(card);  // 즉시 UI 반영

    try {
      await socket.emit('select_card', {roomId, card});
    } catch (error) {
      setMyCard(null);  // 실패 시 롤백
    }
  };

  return { roomState, myCard, selectCard };
};
```

**특징:**
- Optimistic Update로 즉각적 UI 반응
- localStorage에 카드 선택 정보 저장 (새로고침 대응)
- Socket.io 이벤트로 서버와 동기화

### 4. 타입 안전성

모든 Socket.io 이벤트는 공유 타입으로 정의:

```typescript
// shared/types.ts
export interface SelectCardPayload {
  roomId: string;
  card: string;
}

export interface RoomUpdatePayload {
  users: User[];
  game: GameState;
}

// shared/socket-events.ts
export const SocketEvents = {
  SELECT_CARD: 'select_card',
  ROOM_UPDATE: 'room_update',
  // ...
} as const;
```

**이점:**
- 컴파일 타임에 타입 오류 감지
- IDE 자동완성 지원
- 클라이언트-서버 인터페이스 불일치 방지

## 🎮 게임 상태 머신

```
          ┌─────────────┐
          │  SELECTING  │  사용자들이 카드 선택 중
          └──────┬──────┘
                 │ revealCards()
                 ▼
          ┌─────────────┐
          │  REVEALED   │  모든 카드 공개, 평균 표시
          └──────┬──────┘
                 │ resetRound()
                 ▼
          ┌─────────────┐
          │  SELECTING  │  새로운 라운드 시작
          └─────────────┘
```

**상태별 허용 액션:**
- **SELECTING**: 카드 선택/변경, 카드 공개
- **REVEALED**: 카드 변경, 라운드 리셋

**평균 계산 규칙:**
- 숫자 카드만 포함 (`?`, `커피`, 미선택 제외)
- `1/2` 카드는 `0.5`로 계산
- 결과는 소수점 2자리까지 반올림

## 🔧 주요 기술적 결정사항

### 1. 왜 데이터베이스를 사용하지 않나요?

**이유:**
- 간단한 기능에 DB 오버헤드 불필요
- 영구 저장 불필요 (일회성 세션)
- 배포 및 운영 단순화
- 충분한 성능 (메모리 기반)

**트레이드오프:**
- 서버 재시작 시 데이터 손실
- 수평 확장 제한 (단일 서버만 가능)

### 2. 왜 Socket.io를 선택했나요?

**이유:**
- WebSocket + polling fallback (호환성)
- 자동 재연결 지원
- Room 기능 내장
- 브라우저/Node.js 양쪽 지원

**대안:**
- 순수 WebSocket: fallback 없음
- Server-Sent Events: 단방향 통신만 가능

### 3. 왜 Vite를 사용하나요?

**이유:**
- 빠른 개발 서버 시작 (esbuild)
- Hot Module Replacement (HMR)
- TypeScript 네이티브 지원
- 효율적인 프로덕션 빌드

**대안:**
- Create React App: 느린 빌드
- Webpack: 복잡한 설정

### 4. 왜 Tailwind CSS를 사용하나요?

**이유:**
- 유틸리티 우선 접근으로 빠른 개발
- 다크모드 내장 지원
- 반응형 디자인 쉬움
- 프로덕션 빌드 시 사용하지 않는 CSS 제거

## 📊 성능 고려사항

### 메모리 관리

```typescript
// 비활성 방 자동 정리 (5분마다)
setInterval(() => {
  const oneHourAgo = Date.now() - 60 * 60 * 1000;

  rooms.forEach((room, roomId) => {
    if (room.lastActivity < oneHourAgo) {
      rooms.delete(roomId);
    }
  });
}, 5 * 60 * 1000);
```

### 네트워크 최적화

- Gzip 압축 활성화
- 정적 파일 캐싱
- WebSocket 연결 재사용
- Optimistic Update로 왕복 지연 최소화

### 확장성 제한

현재 아키텍처는 **단일 서버 수직 확장**만 지원:
- 동시 접속자: ~1,000명 (추정)
- 동시 방: 최대 10개 (소프트 제한)
- 방당 인원: 최대 20명 (소프트 제한)

**수평 확장을 위해 필요한 변경사항:**
- Redis와 같은 외부 상태 저장소
- Socket.io Redis Adapter
- Load Balancer + Sticky Session

## 🔒 보안 고려사항

### 현재 구현된 보안

1. **입력 검증**: 사용자 이름, 방 이름 길이 제한
2. **XSS 방지**: React의 자동 이스케이핑
3. **CORS 설정**: 허용된 origin만 접근

### 미구현 (추가 필요 시)

1. **Rate Limiting**: DoS 방지
2. **인증/인가**: 현재 없음 (설계상)
3. **데이터 암호화**: 민감 정보 없음

## 📚 참고 자료

- [시작하기](GETTING_STARTED.md) - 로컬 개발 환경
- [Docker 가이드](DOCKER.md) - 컨테이너화
- [배포 가이드](DEPLOYMENT.md) - 프로덕션 배포
- [CLAUDE.md](../CLAUDE.md) - AI 개발 가이드
